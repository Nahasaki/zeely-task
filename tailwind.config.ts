import type { Config } from 'tailwindcss';

const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        sans: ['"Italian Plate No2 Expanded"', 'sans-serif'],
      },
    },
  },
};

export default config;
